import { ZeroOne } from "../../../../../utils/interfaces";

export class DoubleTroubleObject {
  text: ZeroOne;
  color: ZeroOne;
  constructor(text: ZeroOne, color: ZeroOne) {
    this.text = text;
    this.color = color;
  }
}
